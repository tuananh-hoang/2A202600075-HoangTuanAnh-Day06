[build]
builder = "DOCKERFILE"
dockerfilePath = "backend_ai/Dockerfile.railway"
watchPatterns = ["backend_ai/**", "NhomA1-C401-Day06/data_pipeline/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
